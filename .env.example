# ==============================================
# 微信公众号 API 凭证配置示例
# ==============================================
# 1. 复制本文件为 .env 文件：cp .env.example .env
# 2. 填写你的实际凭证信息（注意：.env 文件不会被提交到 Git）
# 3. 获取方式：
#    - 发布 API：登录微信开发者平台 -> 我的业务与服务 -> 公众号
#    - 采集 API：登录 mp.weixin.qq.com -> 浏览器开发者工具（F12） -> Network
#
# 敏感信息配置优先级（从高到低）：
#   1. config.yaml 文件（最高优先级，对应字段有值时优先使用）
#   2. .env 文件（推荐，自动添加到 .gitignore）
#   3. 系统环境变量（最低优先级）
#
# 使用建议：
# - 如果不想在 config.yaml 中暴露凭证，保持 config.yaml 对应字段为空
# - 系统会自动从环境变量读取（先查 .env 文件，再查系统环境变量）
# - 推荐使用 .env 文件管理敏感信息，避免泄露
#
# 注意事项：
# - 所有凭证均为敏感信息，请勿泄露或提交到版本控制
# - token 和 cookie 会定期失效，失效后需要重新获取
# ==============================================

# ==============================================
# 采集 API 凭证（API 模式专属，v2.0.0 新增）
# ==============================================
# 微信公众平台后台 Token（会定期失效）
# 获取方式：登录 mp.weixin.qq.com -> F12 开发者工具 -> Network -> 找到任意请求 -> 查看 Query Parameters 中的 token
WECHAT_API_TOKEN=your_token_here

# 微信公众平台后台 Cookie（会定期失效）
# 获取方式：登录 mp.weixin.qq.com -> F12 开发者工具 -> Network -> 找到任意请求 -> 查看 Request Headers 中的 Cookie
WECHAT_API_COOKIE=your_cookie_here


# ==============================================
# 发布 API 凭证（用于自动发布公众号文章）
# ==============================================
# 公众号 AppID（开发者ID）
WECHAT_APPID=your_appid_here

# 公众号 AppSecret（开发者密码）
WECHAT_APPSECRET=your_appsecret_here



# ==============================================
# 阿里云 DashScope API Key
# ==============================================
# 用于 VLM 图像识别（RPA 模式文章采集）和 LLM 文章摘要生成
# 获取方式：https://dashscope.console.aliyun.com/apiKey
# ==============================================
DASHSCOPE_API_KEY=your_dashscope_api_key_here
